/** Workspace file path constants shared across the persistence layer. */

/** The serialized project inside every workspace/commit file map. */
export const PROJECT_JSON_PATH = 'turnbased.project.json';
